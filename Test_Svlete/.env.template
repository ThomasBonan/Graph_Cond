# ============================================================================
# .env.template
# ----------------------------------------------------------------------------
# Copiez ce fichier en ".env" puis adaptez les valeurs avant de lancer
# l'application (local ou Raspberry Pi). Les variables sont lues à la fois par
# l'API Express et par le client Vite.
# ============================================================================

# Environnement Node. Gardez "development" en local, "production" en déploiement.
NODE_ENV=development

# Port HTTP exposé par l'API Express.
PORT=3000

# Secret utilisé pour signer les JWT. Remplacez par une valeur robuste.
JWT_SECRET=change-me-in-production

# Durée de vie des jetons (secondes). 900 = 15 minutes.
JWT_TTL_SECONDS=900

# Options cookies JWT (adapter en production).
JWT_COOKIE_SECURE=false
JWT_COOKIE_SAMESITE=lax
JWT_COOKIE_TTL_SECONDS=900
JWT_ROLLING=true

# Identifiants bootstrap : compte crée automatiquement au démarrage du serveur.
# Obligatoire pour créer d'autres utilisateurs depuis l'interface.
BOOTSTRAP_USERNAME=admin
BOOTSTRAP_PASSWORD=changeMe123

# Chemin et nom du fichier SQLite (monté en volume dans docker-compose).
DB_DIR=data
DB_FILE=schemas.db

# URL de base consommée par le frontend (ajuster selon reverse proxy / prod).
VITE_API_BASE=http://localhost:3000

